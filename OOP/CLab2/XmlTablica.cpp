#include "XmlTablica.h"

//std::vector<std::pair<Item, Item>> XmlTablica::getDuplicate()
//{
//	std::vector<std::pair<Item, Item>> dups;
//	for (const Item& i : data)
//		for (const Item& j : static_cast<const FItem&>(i).subData)
//			if (static_cast<const DItem&>(j).dups > 0) dups.emplace_back(i, j);
//	return dups;
//}
